{
  "name": "@repo/database",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:push": "prisma migrate dev --skip-generate"
  },
  "exports": {
    "./src": "./src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
